"""Value shapes that cross a boundary: LLM structured output, future API request/response bodies.

Distinct from app.db.models, which describes database tables. A class
here describes data shape and validation, not persistence.
"""
